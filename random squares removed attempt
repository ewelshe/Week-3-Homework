void setup(){
  size (500,500);
  background (255);
  stroke(0);
int x=10;
  while(x<480){
     int y=20;
     while (y<480){
       rect (x,y,10,10);
        y=y+20;
       if(random(1) >3){
 
  }
       x=x+20;
  
   }
   
     
  }
}
